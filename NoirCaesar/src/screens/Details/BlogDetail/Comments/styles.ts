import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: {
    flex: 1,
    paddingBottom: 55,
  },

  flatList: {
    paddingHorizontal: 9,
  },
});

export default styles;
