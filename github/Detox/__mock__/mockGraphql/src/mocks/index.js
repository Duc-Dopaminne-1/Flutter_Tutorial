const CityDto = require('./CityDto');
const Query = require('./mockQuery');
const Mutation = require('./mockMutation');
const Subscription = require('./mockSubscription');

const mocks = {
  CityDto,
  Query,
  Mutation,
  Subscription,
};

module.exports = mocks;
