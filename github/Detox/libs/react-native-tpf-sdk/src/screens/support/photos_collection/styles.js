import { BACKGROUND_COLOR } from '../../../constants/colors';
import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  photosCollectionWrapper: {
    flex: 1,
    backgroundColor: BACKGROUND_COLOR.White
  }
});

export default styles;
