query getSearchTerms(
  $page: Int
  $pageSize: Int
  $where: SearchTermDtoFilter
  $order_by: SearchTermDtoSort
) {
  getSearchTerms(page: $page, pageSize: $pageSize, where: $where, order_by: $order_by) {
    edges {
      keyWord
    }
    totalCount
  }
}
