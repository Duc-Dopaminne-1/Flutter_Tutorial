mutation PayTransaction($input: payTransactionInput!) {
  payTransaction(payTransactionInput: $input) {
    errorCode
    errorMessage
    errorMessageCode
    disablePayment
    ipnUrl
    linkPaymentUrl
    returnUrl
    transactionId
    paymentCode
  }
}
